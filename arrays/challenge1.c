#include <stdio.h>
#include <String.h>

int main(){

    int i;
    int number[5]=   {1, 2, 3, 4, 5};
    int size;

    for ( i = 0; i < 5 ; i++)
    {
        printf("%d \n", number[i]);
    }

    return 0;
}

